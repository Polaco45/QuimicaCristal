<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="server_action_product_template_vendor_bom_cost" model="ir.actions.server">
        <field name="name">Calcular costo proveedor desde LdM</field>
        <field name="model_id" ref="product.model_product_template"/>
        <field name="binding_model_id" ref="product.model_product_template"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code"><![CDATA[
if records:
    action = records.action_vendor_bom_cost()
        ]]></field>
    </record>

    <record id="server_action_product_product_vendor_bom_cost" model="ir.actions.server">
        <field name="name">Calcular costo proveedor desde LdM</field>
        <field name="model_id" ref="product.model_product_product"/>
        <field name="binding_model_id" ref="product.model_product_product"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code"><![CDATA[
if records:
    action = records.action_vendor_bom_cost()
        ]]></field>
    </record>

</odoo>